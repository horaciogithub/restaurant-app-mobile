import { StyleSheet } from 'react-native';

const nuevaOrdenStyles = StyleSheet.create({
    contenido: {
        flexDirection: 'column',
        justifyContent: 'center'
    },
});

export default nuevaOrdenStyles;